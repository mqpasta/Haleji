@model Haleji.BusinessObjects.Vendor
    <!-- Modal -->
<div class="modal fade" id="deleteVendor" tabindex="-1" role="dialog" aria-labelledby="deleteVendorTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <form asp-action="Delete">
                <div class="modal-header">
                    <h5 class="modal-title" id="deleteVendorTitle">Delete Vendor</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <!-- Modal Body -->
                    <input asp-for="VendorId" type="hidden" />
                    <dl class="row">
                        <dt class="col-sm-4">
                            @Html.DisplayNameFor(model => model.VendorName)
                        </dt>
                        <dd class="col-lg">
                            @Html.DisplayFor(model => model.VendorName)
                        </dd>
                    </dl><dl class="row">
                        <dt class="col-sm-4">
                            @Html.DisplayNameFor(model => model.Address)
                        </dt>
                        <dd class="col-lg">
                            @Html.DisplayFor(model => model.Address)
                        </dd>
                    </dl>
                </div>
                <div class="modal-footer">
                    <div class="form-group">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                        <button type="submit" class="btn btn-danger" data-save="modal">Delete</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>


